// import { useLoaderData } from "react-router-dom";
// import AllCard from "./AllCard";
// import { useState } from "react";

const MyCart = () => {
    // const carData =useLoaderData()
    // const [deleteInfo,setDeleteInfo]=useState(carData)
    return (
        <div className="grid grid-cols-1 md:grid-cols-2 max-w-screen-xl mx-auto">
            {/* {
                deleteInfo.map(data => <AllCard key={data._id} deleteInfo={deleteInfo} setDeleteInfo={setDeleteInfo} data={data}></AllCard>)
            } */}
        </div>
    );
};

export default MyCart;